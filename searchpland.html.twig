{% extends 'base.html.twig' %}

{% block body%}
{% include 'menu.html.twig' %}
{{ parent() }}
{% block stylesheests %}
{% endblock %}
<br>
<div class="divforms">
<a class="divbotones" href="{{path ('plan')}}">Crear Plan</a>
<a class="divbotones" href="{{path ('buscarplanm')}}">Crear Punto de Medicion</a>
<br><br><br>

  <fieldset >
  <legend><h3>Plan de Mantenimiento --- Buscar Plan -- Lanzamiento</h3></legend>
    {{ form_start(form, {'action': path ('buscarpland'), 'method': 'POST'}) }}
      Planes Inactivos
<select name='numero' id='numero'>
{% for num in datosplan %}
<option value="{{num.numero}}">{{num.numero}} - {{num.titulo}}</option>
{% endfor %}
</select>
</fieldset>
      {{ form_widget(form.disparar)}}
    {{ form_end(form) }}
</div>
{% endblock %}